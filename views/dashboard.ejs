<%- include('./partials/header.ejs'); -%>
<div class="container2" id="bodyid2">
    <a href="/">Logout</a>
</div>
<div id="thegrid" class="gridcontainer">
    <%gridData.forEach(movie => {%>
      <ul id="gridiron" class="ulgrid">
          <form action="/dashboard" method="POST">
          <li class="moviegrid">
            <span id="titleid"><div><%=movie.title%></div></span>
            <img id="<%= movie.id %> " class="movimage" src="https://image.tmdb.org/t/p/w1280<%=movie.poster_path%>" alt="<%=movie.title%>"/>
            <input type="submit" value="Save movies"/>
            <input type="hidden" name="title" value="<%= movie.title %> "/>
            <input type="hidden" name="genre" value="<%= movie.genre %> "/>
              <div class="beltdiv">Popularity: <%=movie.popularity%></div>                    
              <div class="truncate"><span id="titleid"><%=movie.overview%></span></div>
              <div><span id="changecol">Release Date: </span><%=movie.release_date%></div>
              <div class="avote"><span class="avote">Average Vote: </span><span id="roundaverage"><%=movie.vote_average%></span></div>
          </li>
      </ul>
    </form>
    <% });%>
</div>

<%- include('./partials/footer.ejs'); -%>